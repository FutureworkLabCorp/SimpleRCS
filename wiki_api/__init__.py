"""SimpleRCS Wiki API — FastAPI backend for the file-system-based wiki POC.

See GPC-188 (Linear) and the Slack thread it originated from for background.
This package is an *application built on top of* `simple_rcs`, not part of
the `simple_rcs` distribution — it is intentionally left out of
`[tool.setuptools]` packaging so the library keeps shipping standalone.
"""
